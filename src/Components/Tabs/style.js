import {StyleSheet} from 'react-native';

export const style = StyleSheet.create({
  TabsContainer: {
    height: 100,
  },
  test: {
    backgroundColor: 'red',
    marginBottom: 20,
  },

  absoluteFill:{
    backgroundImage
  }
});
